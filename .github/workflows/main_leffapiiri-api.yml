name: Build and deploy Backend to Azure Web App (OIDC)

on:
  push:
    branches:
      - main
    paths:
      - 'server/**' # Käynnistyy vain kun backend-koodia muutetaan
  workflow_dispatch:

permissions:
  id-token: write # Vaaditaan OIDC JWT-tokenin pyytämiseen
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: server

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json

      - name: Install dependencies and build
        run: |
          npm ci
          npm run build --if-present

      # Tarkistusvaihe: Varmistaa näkeekö GitHub Actions secret-muuttujat
      - name: Debug Secret Availability
        run: |
          if [ -z "${{ secrets.AZURE_CLIENT_ID }}" ]; then echo "CLIENT_ID is EMPTY"; else echo "CLIENT_ID is set"; fi
          if [ -z "${{ secrets.AZURE_TENANT_ID }}" ]; then echo "TENANT_ID is EMPTY"; else echo "TENANT_ID is set"; fi
          if [ -z "${{ secrets.AZURE_SUBSCRIPTION_ID }}" ]; then echo "SUBSCRIPTION_ID is EMPTY"; else echo "SUBSCRIPTION_ID is set"; fi

      - name: Log in to Azure using OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'Leffapiiri-api'
          package: '.' # Julkaistaan server-kansion sisältö
